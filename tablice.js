const fruits = "Jabłko", "Banan", "Pomarańcza", "Winogrona", "Arbuz"; 


const numbers = [2, 6, 8, 3, 10, 4, 7]; 
for (let i = 0; i < numbers.length; i++) { 
    if (numbers[i] > 5) { 
        console.log(numbers[i]); 

    } 
}
